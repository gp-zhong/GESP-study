<!--pages/profile/settings.wxml-->
<view class="container">
  <view class="header">
    <view class="back-btn" bindtap="navigateBack">
      <image src="/assets/icons/back.png" mode="aspectFit"></image>
    </view>
    <view class="header-title">API设置</view>
  </view>
  
  <view class="settings-section">
    <view class="section-title">API KEY</view>
    <view class="input-container">
      <input 
        class="settings-input" 
        type="password" 
        placeholder="请输入DeepSeek API KEY" 
        value="{{apiInfo.key}}"
        bindinput="onKeyChange"
      />
    </view>
    
    <view class="section-title">API端点</view>
    <view class="input-container">
      <input 
        class="settings-input" 
        placeholder="请输入API端点URL" 
        value="{{apiInfo.endpoint}}"
        bindinput="onEndpointChange"
      />
    </view>
    
    <view class="section-title">API模型</view>
    <view class="picker-container">
      <picker 
        bindchange="onModelChange" 
        value="{{modelIndex}}" 
        range="{{modelOptions}}"
      >
        <view class="picker">
          {{modelOptions[modelIndex]}}
          <image class="dropdown-icon" src="/assets/icons/dropdown.png" mode="aspectFit"></image>
        </view>
      </picker>
    </view>
  </view>
  
  <view class="actions">
    <button class="save-btn" bindtap="saveSettings">保存设置</button>
  </view>
  
  <view class="note-section">
    <view class="note-title">注意事项：</view>
    <view class="note-content">
      1. API KEY为敏感信息，请妥善保管
      2. 默认端点为https://api.deepseek.com/v1
      3. 请确保网络环境稳定，以便正常使用AI功能
    </view>
  </view>
</view>